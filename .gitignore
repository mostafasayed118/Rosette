node_modules/
.next/
out/
coverage/
*.tsbuildinfo
.env*
!.env.example
.dev.vars
.open-next/
.wrangler/
.worktrees/
.claude/

# Python bytecode cache
__pycache__/

dev-restart.log
.freebuff/

.superpowers/

# Vendored agent tooling. 670 files / 13.0 MB — 80% of the tracked repository by
# size and none of it is a build input. Was committed by accident; the equivalent
# material lives in a package manager, not in this repo.
.agents/
docs/superpowers/

# WorkBuddy session state (memory, task lists, cache). Per-developer, never shared.
.workbuddy-ai/

# Stitch design export (source for /docs/stitch-export, not a build input)
stitch_rosette_floral_e_commerce_system/
